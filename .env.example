APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:prEtHhZ82V3u1fWnEqJ6eDXU3nUwx9vTDVuYXqAM03w=
APP_DEBUG=true
APP_URL=http://127.0.0.1
SANCTUM_STATEFUL_DOMAINS=localhost,127.0.0.1,127.0.0.1:8000,localhost:8080,127.0.0.1:8080
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=capstone
DB_USERNAME=root
DB_PASSWORD=

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
# Evidence documents and role-storage vaults. Keep `local` until R2 credentials
# are set and `php artisan storage:migrate-evidence` has been verified, then
# switch to `s3`. Profile photos (`public`) and Dean/instrument files (`private`)
# are not affected.
EVIDENCE_DISK=local
QUEUE_CONNECTION=sync

CACHE_STORE=file
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=adams.archiving.system@gmail.com
MAIL_PASSWORD=vzxtgpnfannufqup
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=adams.archiving.system@gmail.com
MAIL_FROM_NAME="ADAMS Archiving System"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=auto
AWS_BUCKET=
AWS_URL=
AWS_ENDPOINT=
AWS_USE_PATH_STYLE_ENDPOINT=false
R2_ACCOUNT_ID=
# If AWS_ENDPOINT is empty, it is derived as https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com
DOCUMENT_UPLOAD_MAX_KB=51200
MEDIA_UPLOAD_MAX_KB=1048576
UPLOAD_CHUNK_SIZE_BYTES=8388608
UPLOAD_CHUNK_THRESHOLD_BYTES=52428800

VITE_APP_NAME="${APP_NAME}"